#lang info

(define collection "deferred")

(define name "deferred")
(define blurb
  '("A simple deferred task library for running functions at a specified time "
    "or after a specified delay."))
(define primary-file "main.rkt")
(define homepage "https://github.com/cjfuller/deferred")
(define version "0.1.1")
(define scribblings '(("deferred.scrbl" ())))
